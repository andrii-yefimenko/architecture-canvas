## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`andrii-yefimenko/architecture-canvas`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` and `docs/adr/` at the repo root, created lazily as decisions are made. See `docs/agents/domain.md`.

### Plan

Roadmap, hard constraints, and the Decision Log live in `docs/02-PLAN.md`; the Backlog lives separately in `docs/03-BACKLOG.md`. See `docs/agents/plan.md`.
